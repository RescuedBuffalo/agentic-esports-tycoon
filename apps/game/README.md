# apps/game

Player-facing game client. **Empty stub** — populated in a later phase.

The headless simulation lives under `services/`; this directory is reserved
for whatever surface (web, desktop, tooling) ends up consuming it.
